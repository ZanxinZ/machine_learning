前向传递就是计算公式，输入x 和权重w的内积加上一个偏置bias
损失L，对权重w求偏导；偏导数大于0时，L在增大，需要减小；偏导数小于0时，L需要增大；偏导数接近0时。

激活函数
# sigmoid
def sigmoid(x):
    return 1 / (1 + np.exp(-x))

简单网络的的搭建

训练的过程：w1,w2,w3,w4,w5,w6,b1,b2,b3的改变引起了L的改变，所以，要使得各个参数往L减少的方向变化
